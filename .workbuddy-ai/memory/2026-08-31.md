# 2026-08-31

## Codebase review (full audit)
Wrote `CODE_REVIEW.md` at repo root. Verified findings myself — did not rely on agent
reports alone. Confirmed commands:

- `npm run build` → PASS, 9 pages, ~11s
- `npx vitest run` → 30/30 pass, BUT vitest.config.ts `include` only covers `src/` + `e2e/`.
  13 backend suites under `netlify/functions/_lib/` are silently excluded. Running them
  (temp config, env=node) → **92 passed, 4 failed, 1 file broken**.
- `npx tsc --noEmit` → 50 errors, ALL in `src/types/api.ts` (corrupted file: stray `*/`,
  `~//`, `nexport`). TS bails on semantic analysis when syntax errors exist ⇒ **entire
  `src/` is effectively un-type-checked**. File is never imported → deleting it is free.
- `npx vitest run --reporter=basic` and `--include` are NOT valid flags in vitest 4.x.
  Use a separate config file with `--config` instead.

### Key confirmed defects
- `shared/wa-rules.ts`: `normalizeWa('812…')` returns `''` (bare `8xx` unhandled);
  `isValidWaFormat` regex `/^628\d{10,11}$/` demands 13-14 digits but the user-facing
  error string in `actions-auth.ts:125` promises "12-13 digit" ⇒ valid 12-digit numbers
  are rejected at login/register.
- Admin panel: `/admin` is static HTML, `AuthGuard.tsx` never imported (dead), gate is
  only a localStorage `asj_auth` atom.
- `handleGetUploadUrls` (actions-upload.ts:87) — no auth, service-role signed URL, `../`
  survives sanitisation. `handleCekDataPelamar` (:160) + `handleGetDaftarSiswaBaru`
  (actions-register.ts:11) — unauthenticated PII dumps.
- IDOR: actions-master.ts:707 (read), :917 (write), ai/cv.ts:204 + :482 (CV + e-signature
  forgery; `const guard = kandidatGuard;` at cv.ts:219 is assigned then never used).
- `session.ts:12-22` falls back to literal `'asj-portal-local-secret'` committed in repo.
- `src/lib/fcm.ts:94` imports non-existent `callAPI` from apiClient ⇒ push never works.
- `MasterFullForm.tsx:87,96` reads `auth.token` (field is `sessionToken`) + partial atom
  write that drops isLoggedIn/role.
- `netlify/functions/.netlify-built/` = 167,849 lines, committed to git, NOT gitignored.
- 24 `.js`/`.cjs` pairs are byte-identical (verified via diff).

### Repo facts
- `src/` 68 files / 10.5k lines · `_lib/` 51 files / 11.5k lines · `legacy/` 223 files /
  36.5k lines (pre-Astro migration, unreviewed).
- Stray untracked dirs at root: `.agents/`, `.freebuff/` — neither gitignored.
- Temp file `vitest.backend.config.ts` was created for the audit and deleted afterwards.

## Notes
- No fixes were applied — review only. User asked to "check code base".
