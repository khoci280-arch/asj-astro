# ✅ Checklist Parity 1:1 — Modal & Button (legacy live ↔ Astro v2)

> **Cara pakai:** satu baris = satu unit. Crosscheck "sampai akar": (1) pemicu button/event
> wired di UI Astro; (2) field/state 1:1 dgn legacy (`khoci921/js/*` + `khoci921/docs/*-DEEP.md`);
> (3) action + endpoint + payload + session cocok dgn handler backend (kontrak legacy);
> (4) response diproses benar (success/error/session); (5) tabel/efek samping benar; (6) i18n
> ID/JP ada; (7) improve bila jelas (codebase modern).
> Status: ⬜ belum dicek · 🔄 sedang · ✅ selesai (tanggal) · ⚠️ ditemukan delta (link catatan)
> Acuan matriks fitur: `docs/LEGACY_PARITY_REFERENCE.md`; delta form: `docs/PARITY_QA_2026-09-04.md`.

## A. Modal Admin (panel `admin.html` ↔ `admin.astro`)

| # | Modal / aksi (legacy trigger → file legacy) | Komponen Astro | Status | Catatan |
|---|---|---|---|---|
| A01 | Cek Data siswa (`bukaModalCekDataSiswa` → `admin_ops/candidates.ts`) | `CekSiswaModal.tsx` | ✅ 2026-09-04 | Rebuild lama salah sumber data (`getAppData 'siswa'`) & field (wa/status/kelas); dikoreksi ke `getDaftarSiswaBaru` + tabel No/Nama/JK/Alamat + state session/error |
| A02 | Profil kandidat (`bukaModalKandidat`; dossier `modal-cv` → `simpanCatatanCv`) | `CandidateProfileModal.tsx` | ✅ 2026-09-04 | Sumber data dikoreksi: row ter-dekorasi `getCandidatesPage` dikirim lewat event (sebelumnya `getAppData 'kandidat'` ditolak sesi admin → selalu kosong); fallback = `getExistingCandidateJsonByWa`; tombol Simpan Evaluasi di-wire ke `updateCatatanKandidat` (catatan_internal/external + tag [VIP], colom internal/ext kini benar-benar ditulis backend); badge [VIP] vs KELAS dipisah (`mapCandidate`); tile JFT/SSW = nilai teks (bukan URL), fisik = TB/BB; refresh daftar via event `candidates-changed` |
| A03 | Tambah/edit kandidat (`bukaModalTambahKandidat`/`prosesUploadKandidat` + `bukaSuperEditKandidat`/`simpanSuperEditKandidat`, `js/api/candidates.ts`) | `EditCandidateModal.tsx` + `InputManualModal.tsx` | ✅ 2026-09-04 | Root-fix: (1) InputManual kirim raw FormData tanpa action → no-op "pong"; kini Cloudinary → JSON `simpanKandidatDanUpload` + dokumen lain via `simpanBerkasTahapan`; (2) EditCandidateModal baca field lama (tmplahir/fisik/jft-URL) → prefill kosong & nilai salah; kini field mapCandidate (tempatLahir/tglLahir/tb/bb/jftText/sswText) + usia auto dari tglLahir; (3) payload super-edit drop pendidikan/catatanExt/isVip → backend kini persist `pendidikan`+`catatan_external`+tag [VIP] di `catatan_internal` (VIP tidak lagi nempel di catatan_admin); (4) upload dokumen dulu hanya ke Cloudinary tanpa persist → kini `simpanBerkasTahapan`; (5) kolom Catatan tabel = `catatanExt‖catatan` (parity legacy); refresh via `candidates-changed` |
| A04 | List kandidat per job (`bukaModalListKandidat` → `js/render/admin.ts` count cell; `keluarkanKandidatDariJob`/`mulaiKirimUndanganGrup`, `js/admin_ops/candidates.ts`) | `ListKandidatModal.tsx` | ✅ 2026-09-04 | Root-fix: (1) data lama hanya filter halaman store (≤20 baris) → count/list salah; kini `fetchAllKandidat()` (loop `getCandidatesPage` 200/halaman) di store + refresh tiap buka, count cell TabDbJob ikut benar; (2) tombol 👁 dossier legacy (`bukaDigitalCV`) ditambahkan → event `showCandidateHistory`; (3) Undang Grup kirim payload array [waList,pesan,interval] yang salah + di-queue ke worker `wa.broadcast` yang **NOT_IMPL** → tidak pernah terkirim; kini payload object legacy `{candidates,jobCode,linkGrup,interval}` + worker sweep-queue benar-benar mengirim via `handleKirimTawaranMassal`; (4) remove → `tandaiGagalJob [wa,code]` (backend sudah guard admin) + refresh store penuh |
| A05 | Pemberkasan checklist (`bukaModalPemberkasan`/`prosesUploadPemberkasan`/`prosesSimpanBiodataLengkap`, `js/03_candidate.ts` + `#modal-pemberkasan`) | `PemberkasanModal.tsx` | ✅ 2026-09-04 | Root-fix: (1) `jenisBerkas` lama = `id.toUpperCase()` ('SD','UNIV','CERT','FOTO2','IJINORTU','KAWIN','SEHAT') — token TIDAK ada di FILE_LABEL_COLUMNS backend → mayoritas dokumen di-upload ke Cloudinary tapi TIDAK di-persist; kini token kanonik backend lewat `src/lib/berkasCatalog.ts` (shared modal + dashboard) + alias label legacy ditambahkan di backend; (2) `simpanBiodataLengkap` NOT_IMPL di surface → biodata tidak pernah tersimpan; kini handler nyata di contexts/master-data (guard session + owner-or-admin sebelum DB) + surface MASTER_ACTIONS; (3) tanpa prefill: checklist Sudah/Belum per dokumen, auto-fill biodata c.bio (short→long keys, tgllahir DD/MM/YYYY→ISO), gating panel per tahapan kandidat, konfirmasi timpa file lama, upload serial w/ retry 3x backoff; (4) CandidateDash baca `kandidatData` yang tidak pernah dikembalikan backend (bentuk asli = `candidates[0]` ter-dekorasi) → dash kosong & progres pemberkasan palsu; kini adapter ke bentuk asli + prefill modal + refresh `candidates-changed`. +7 test backend (`service-a05.test.ts`), +4 test frontend (PemberkasanModal); backend 25 file/232 test, frontend 8 file/58 test hijau |
| A06 | Undangan kelas WA — grup orang tua/wali (`bukaModalUndanganKelas`/`previewUndanganKelas`/`kirimUndanganKelas`, `js/admin_ops/candidates.ts` + `#modal-undangan-kelas`) | `UndanganKelasModal.tsx` | ✅ 2026-09-04 | Root-fix: (1) `parseDaftarOrtu` hanya menerima 628… — baris 08xx/8xx yang valid di legacy (`normalizeWaInput`) dihitung invalid & dibuang; kini normalize via `shared/wa-rules` (parity legacy) + fns di-export untuk test; (2) `kirimTawaranMassal` di rebuild = async-accepted (job queue wa.broadcast), bukan sinkron → modal lama membaca `res.results` kosong → toast "0 undangan terkirim" menyesatkan; kini: accepted → toast antrean (job id), polling `getJobStatus` (6 dtk, cap ±9 mnt) sampai done → ringkasan hasil akhir (hasil job di-persist oleh `recordJobResult` di sweep-queue, sebelumnya hasil handler dibuang oleh completeJob); (3) placeholder i18n + key hilang (`ui.sending`, `ui.group_link_placeholder`, `ui.start_invite`, `ui.toast_invites_queued`; `toast_invalid_rows_n` disinkron ke teks legacy); (4) ikon header modal `link`/`stopwatch`/`comment-dots` tidak ada di sprite (builder hanya scan JSX `<Icon name=…/>`, bukan hyperscript `h(Icon, { name: … })`) → scanner diperluas + sprite di-regenerate; (5) mapping `getJobStatus` hilang di `apiEndpoint.ts` → ditambah. +8 test frontend (parser parity + preview); frontend 9 file/66 test, backend 25 file/232 test hijau |
| A07 | TTD (esign) (`bukaModalTtd`/`bukaLayarCanvas`/`submitDataEsignFull`, `js/12_esign_match.ts` + `#modal-ttd` + `#modal-fs-canvas`) | `EsignNaiteiModal.tsx` (baru; `ESignatureModal.tsx` lama orfan) | ✅ 2026-09-04 | Root-fix: (1) rebuild lama cuma SATU area tanda tangan (`saveSignature` → ttd1 saja); legacy `#modal-ttd` punya 4 area — ttd1+nama1 (Pihak 1) & ttd2+nama2 (Pihak 2/Wali), tiap area digambar di layar penuh canvas (area Nama kanvas lebar + hint rotate HP); kini `EsignNaiteiModal` mem-port penuh → submit sekali `simpanDataTtdNaitei {wa,ttd1,nama1,ttd2,nama2}` base64 dataUrl + event `candidates-changed`; (2) handler backend hanya `requireRole('kandidat')` → admin ditolak, dan hanya terima objek padahal callAPI kirim ARRAY args → wa tak pernah ketemu; kini verifyToken kandidat/admin + unwrap array (objek legacy GAS & array args dua-duanya diterima), scope owner-or-admin + cek IDOR tetap dipertahankan (semua rejection sebelum DB); (3) tombol E-Sign CandidateDash di-wire ke modal + gating tahapan = regex legacy bukaModalTtd (LOLOS..NAITEI, `allowedTahapanEsign` di-export utk test); (4) ~24 kunci i18n baru (area 2 pihak, hint, esign.clear/save, toast); (5) ikon canvas (file-signature/pen/users/eraser/…) masuk sprite; komponen menghindari literal nama ikon dalam ekspresi (spt `tone === 'sky'`) → false-positive scanner turun 2→1. +6 test backend (`service-a07.test.ts`), +8 test frontend (EsignNaiteiModal); frontend 10 file/74 test, backend 26 file/238 test hijau |
| A08 | Ganti password (`bukaModalGantiPass`/`prosesGantiPasswordKandidat`, `js/04_auth.ts` + `#modal-ganti-pass`; backend `_lib/actions-auth.ts handleGantiPasswordKandidat`) | `ChangePasswordModal.tsx` | ✅ 2026-09-05 | Root-fix: (1) **PATCH selalu gagal** — Astro menulis `password_diubah: <ISO timestamp>` padahal kolom live boolean (legacy `true`; `row-types.ts` setuju `password_diubah?: boolean`) → PostgREST tolak seluruh PATCH; kini `buildPasswordPatch()` (pure, di-test) → `{ password_kandidat: hash, password_diubah: true }`; (2) **raw fetch tanpa session token** → surface selalu balas 'Akses ditolak.'; kini lewat `apiClient` (Bearer + body sessionToken, sessionInvalid → toast+redirect); (3) **guard admin** (`isOwnerOrAdmin`) — legacy kandidat-self ONLY (`t.role==='kandidat' && t.wa===wa` → `sessionInvalid` sebelum DB); kini `requireRole('kandidat')` + cek WA (admin/IDOR/refresh ditolak, di-test DB-free); (4) **validasi baru** = `passwordField` (min 4) → schema `newPasswordField` 6–20 tanpa spasi di zod server (`schemas.gantiPassword`) + klien persis urutan legacy (isi semua → cocok → 6-20/no-space → hint); (5) klien baca `data.error` yang tak pernah dikirim Astro → kini `data.message || data.error`; (6) copy hard-coded → `t('changepass.*')` (title/old/new/confirm/btn/loading/ok baru), hint disinkron ke teks legacy `ui.pass_new_hint` (6-20, tanpa spasi, ≠4 digit terakhir WA) id+jp; (7) autocomplete current/new-password + placeholder legacy. +8 test backend (`service-a08.test.ts`), +6 test frontend (ChangePasswordModal); backend 27 file/246 test, frontend 12 file/84 test, typecheck exit 0 |
| A09 | CV mini (`bukaModalCvMini`/`prosesSimpanCvMini`, `js/03_candidate.ts` + `#modal-cv-mini`; simpanUpdateMaster → handler sama dgn submitMasterForm) | `CvMiniModal.tsx` | ✅ 2026-09-05 | Root-fix: (1) **raw fetch tanpa session token** → surface master selalu sessionInvalid (defect class A08); kini `api.secure('simpanUpdateMaster', [payload])`; (2) **tanpa prefill** — modal buka kosong/default padahal row kandidat ter-dekorasi sudah dibawa CandidateDash (gender/usia/tb/bb/pendidikan/jftText/sswText); legacy bukaModalCvMini isi dari baris sendiri; kini prefill via prop (normalisasi gender PRIA/L→LAKI-LAKI & WANITA/P→PEREMPUAN, usia/tb/bb digit-only, helper murni `normalizeGender`/`digitsOnly`/`pendidikanLevel` di-export utk test); (3) **pendidikan free-text** → select tetap legacy (SMA/SMK/MA/D3/S1 + placeholder); nilai '-' TIDAK dikirim (hindari timpa `pendidikan_1_tingkat` dgn '-'); (4) **foto key `photo` dibuang diam-diam** oleh handler bersama (MASTER_FILE_COLUMNS mengharapkan `photoFile`→pas_photo — bug yg juga ada di legacy) → upgrade: kirim `photoFile` URL Cloudinary → PAS FOTO CV Mini benar-benar persist + sinkron ke baris kandidat; (5) **tanpa refresh** setelah simpan (legacy refreshDataDinamis) → dispatch `candidates-changed` (CandidateDash reload, listener A05); (6) copy hard-coded/umum → `ui.update_cv_mini` + kunci baru id+jp (`ui.master_update_hint`, `ui.save_cv_mini`, `ui.toast_cvmini_updated`, `ui.latest_photo`, `cvmini.pilih_pendidikan`; `cvmini.ssw` disinkron ke makna legacy 'Bidang SSW'); guard backend (anon/refresh/IDOR/WA kosong) di-pin DB-free. +5 test backend (`service-a09.test.ts`), +7 test frontend (CvMiniModal); backend 28 file/251 test, frontend 13 file/91 test, typecheck exit 0 |
| A10 | Preview CV / Rirekisho (`bukaPreviewCV`/`bukaPreviewCV_Admin` → `renderCVAjaib`, `js/10_cv_rirekisho.ts` + `10b_cv_builders.ts` + `#modal-preview-cv`) | `RirekishoBuilder.tsx` | ✅ 2026-09-05 | Root-fix: (1) **tombol kandidat "Preview Desain CV" membuka `DocumentPreviewModal` dgn URL KOSONG** (tidak menampilkan apa pun — modal fallback "Tidak bisa dipratinjau"); legacy bukaPreviewCV membuka preview CV Rirekisho (A4); kini tombol membuka `RirekishoBuilder` (waTarget = sesi sendiri, wa yg sama dgn data dashboard) — engine modern yg setara renderCVAjaib; (2) **foto**: legacy renderCVAjaib pakai uploads.photo master lalu fallback `pasPhoto` baris kandidat (ALL_CANDIDATES); builder Astro hanya baca uploads.photo → tanpa fallback (foto kosong padahal pas_photo ada); kini prop `fotoFallback` (TabPelamar & CandidateDash meneruskan `row.pasPhoto`; tipe store `Kandidat` + `pasPhoto`); (3) **bug tanggal/format rirekisho**: regex hasil port kehilangan backslash — `\d` jadi literal `d`, `\D` jadi literal `D` → `fmtMonthYearJp('2024')` (tahun saja) = `2024年1月` (salah, harus `2024年`), strip non-digit usia/TB/BB/no-HP (`/D/g`) & nomor rirekisho `P - xxxx` (`/(d{3,})$/`) tak pernah jalan; dikoreksi ke `\d`/`\D` (parity js/helpers_cv.ts & 10b); (4) loading copy hard-coded 'Loading...' → `t('ui.loading')`. +15 test frontend (`helpers_cv.test.ts` 9: fmtMonthYearJp tahun/bulan, mergeArrRiwayat union+dedupe, esc, getPath/makeV; `RirekishoBuilder.test.tsx` 6: preview kandidat MODE PREVIEW tanpa cetak + tanggal JP `1995年08月14日`, admin tombol Cetak/PDF, error server asli, fotoFallback & prioritas uploads.photo, isOpen=false); frontend 15 file/106 test, backend 28 file/251 test, typecheck exit 0 |
| A11 | Admin AI Copilot (`bukaAdminAiCopilot`, `ai_copilot/admin` + `ai_copilot/parse` + `ai_copilot/results`, `#modal-admin-ai`) | `AdminAiCopilot.tsx` | ✅ 2026-09-05 | Root-fix: (1) **chat bubble tak pernah dirender** — pesan dikumpulkan ke state tapi JSX cuma menampilkan typing indicator (chat tampak mati); kini bubbles dirender (esc + **bold** → `<b>`, parity `tambahPesanAdminAi`); (2) **semua aksi raw fetch tanpa session token** → guard surface selalu sessionInvalid (defect class A08/A09); kini lewat `api.secure` (Bearer + body, sessionInvalid/network terpusat); (3) **parse mati di backend**: `surfaces/ingest.ts` men-queue job `ingest.parse` yang worker-nya `NOT_IMPL` di sweep-queue, sementara handler asli `_lib/ai/classify.ts handleParseDokumenBiodata` (guard admin → Gemini → `{wa,data,fieldCount,fileName,namaSekarang,riwayat}`) jadi orfan; kini surface memanggil handler asli sinkron (kontrak legacy) + worker NOT_IMPL dihapus; (4) **parse legacy dua langkah** (`parseDokumenBiodata` → `submitMasterForm({wa, ...data})`) — modal lama hanya langkah 1 lalu membuang data hasil parse (toast palsu "0 field"); alur dua-langkah dipulihkan + `candidates-changed` (refresh tabel pelamar); (5) **candidateId salah**: TabPelamar dispatch tanpa id & AdminPanel mengirim `candidateId = nama`; kini detail `{id, wa, nama}` & prop `candidateId = id` (backend resolve by `id_kandidat`); (6) hasil wawancara ringkas → parity legacy (updatedAt/nilai/bio field count + backfill WA); (7) copy hard-coded → 18 key baru id+jp (`ui.ai_copilot`, tab/upload/btn/status/results, `ai.parse_ok_title/bidang_label`), label menu App.tsx ikut `t('ui.ai_copilot')`; chip saran cek backend: `handleProcessAdminAIChat` bebas keyword-parser (free-form Gemini) → terjemahan aman. Bonus residual A10: `public/LokerTable.tsx` fallback-sort `replace(/D/g)` → `replace(/\D/g)` (parity `js/render/{admin,public}.ts`). +6 test backend (`service-a11.test.ts` — route sync non-job + guard anon/kandidat/refresh/validasi file DB-free), +9 test frontend (AdminAiCopilot + boldHtml); backend 29 file/257 test, frontend 16 file/115 test, typecheck exit 0 |
| A12 | Rincian builder (`openRincianBuilder`, `13_rincian_builder`) | `RincianBiayaModal.tsx` (baru; dulu `InputManualModal.tsx` keliru — builder tak pernah ada di Astro) | ✅ 2026-09-05 | Root-fix: (1) **tombol "Buka Editor Rincian" di TabTambah MATI** (tanpa onClick) & rincian_biaya tak pernah disimpan (hanya totalBiaya terkirim); komponen builder baru `RincianBiayaModal.tsx` (port `#modal-rincian-builder` + `js/13_rincian_builder.ts`: rbSerialize/rbSeedFromText/save-unsave preset) dan TabTambah kini mengirim `totalBiaya` + `rincianBiaya` via `api.secure('simpanJobBaru')` — bukan raw multipart POST action fiktif `submitFormAdmin` yang tidak ada di surface (backend map `simpanJobBaru`); (2) **`AdminJobEditModal` tanpa field total/rincian sama sekali** (legacy edit form punya ef-total-biaya/ef-rincian-biaya) — kini kolom totalBiaya + textarea rincian + tombol buka builder; (3) **preset collection endpoints ada di backend tapi tak pernah dipanggil UI** (`getRincianPresets`/`saveRincianPreset`/`deleteRincianPreset` di surface config) — modal kini load preset DB (fallback DEFAULT_PRESETS legacy) + favorite star save/delete; (4) format teks rincian kini round-trip stabil dgn parser publik `LokerDetailModal.parseRincianBiaya` (TOTAL BIAYA / TAHAPAN PEMBAYARAN / INCLUDE-EXCLUDE-BENEFIT-PERSYARATAN / CATATAN) via helper murni `rincianSerialize`/`parseRincianState`/`fmtNominal`/`rincianSummary` (di-export utk test); (5) copy hard-coded → ~21 key baru id+jp (judul/modal/btn/preset/stars/status/summary). +5 test backend (`service-a12.test.ts` — preset endpoints guard anon/kandidat/refresh-token + validasi payload DB-free), +10 test frontend (`RincianBiayaModal.test.tsx` — serialize round-trip, seed, preset load, favorite save/delete, custom chip, initialTotal); backend 30 file/262 test, frontend 17 file/125 test, typecheck exit 0 |
| A13 | Laporan bulanan (`showMonthlyReport`) | `LaporanBulananModal.tsx` | ✅ 2026-09-05 | Root-fix: (1) **modal TIDAK PERNAH memanggil backend** — dulu meng-agregasi store `kandidatList` klien (hanya halaman aktif yang termuat/terfilter, ±20–50 baris) jadi "laporan" = subset tak lengkap & tak pernah sama dengan legacy; legacy `showMonthlyReport` memanggil `getMonthlyReport` (backend agregasi SEMUA kandidat — `handleGetMonthlyReport` Astro sudah jadi port setia: guard admin → dedupe → group per loker + tahapan + status, respons `{success, report[], totalCandidates, generatedAt}`). Kini modal fetch via `api.secure('getMonthlyReport')` + state loading/error; (2) **header metadata (totalCandidates + tanggal generatedAt) dan empty-state tidak ada** — kini dirender (Total: N · YYYY-MM-DD; `admin.report_empty` bila kosong); (3) **semua copy hard-coded Indonesia** (judul "Laporan Kandidat per Loker", Total, Per Loker/Per Tahapan/Per Status, Tutup) → 6 key baru id+jp (`admin.monthly_report/report_title/report_total/report_by_stage/report_by_status/report_empty` — nilai JP dari `assets/jp-locale.js` legacy: 月次レポート/職種別候補者レポート/合計/選考段階別/ステータス別/候補者データがありません。), tombol pemicu TabPelamar "Laporan Bulanan" ikut `t('admin.monthly_report')`; (4) kartu per-loker kini chip tahapan+status (parity legacy), title announce via info-toast. +6 test frontend (`LaporanBulananModal.test.tsx` — closed tak fetch, open → `getMonthlyReport` + render data server + header tanggal, empty state, error server/toast asli, network catch, close); backend tak berubah (30 file/262 test), frontend 18 file/131 test, typecheck exit 0 |
| A14 | Matchmaking AI (`bukaMatchmaking`/`jalankanMatchmaking`/`kirimTawaranMassal`, `js/12_esign_match.ts` + `#modal-matchmaking`) | `MatchmakingModal.tsx` | ✅ 2026-09-05 | Root-fix: (1) **rule "belum terdaftar di Job ini" hilang** — legacy mengecualikan kandidat yg `idLoker`-nya sudah memuat kode job ini (jangan menawari yg sudah mendaftar); Astro lama tidak cek sama sekali → blast bisa menawari kandidat yang sudah di job tsb; (2) **sertifikat dibaca dari field FILE (`jft`/`ssw` URL) padahal legacy pakai teks nilai `jftText`/`sswText`** — sort prioritas kelengkapan, aturan "Wajib JFT/SSW", & badge hasil kini konsisten pakai teks (file terisi tapi teks `-` tidak dianggap punya sertifikat); (3) **gender auto-fill hanya kenal LAKI/PEREMPUAN** — legacy juga PRIA/WANITA (keduanya kini didukung; helper `genderFromJob` di-export); (4) **hasil render tanpa batas** — legacy tampil maks 30; (5) **blast raw fetch tanpa session & tanpa `linkGrup`/`customMessage`** (kontrak legacy `{candidates, jobCode, linkGrup, customMessage}`) + tanpa konfirmasi — kini `api.secure('kirimTawaranMassal')` + payload lengkap + `window.confirm` parity legacy; (6) **copy semua hard-coded** → ~26 key baru id+jp (label/title/kriteria/filter/result/no-match/toast + template pesan offer + `admin.btn_match` utk tombol Match TabDbJob); jp juga menambah 3 key toast offer yg semula hanya ada di id. +12 test frontend (`MatchmakingModal.test.tsx` + helper) |
| A15 | Share settings job (`bukaModalShare`/`renderShareCheckboxes`/`simpanDokumenShare`/`templateShareWa`/`copyShareLink`/`copasShareWa`, `js/render/share.ts` + `partials/modals-shared.html #modal-share-loker`) | `AdminShareModal.tsx` | ✅ 2026-09-05 | Root-fix: (1) **endpoint share view MATI total** — `netlify/functions/share-data.js` masih stub NOT_IMPLEMENTED padahal handler nyata `handleShareData` (publik guard-less, cari job + dokumen kandidat) sudah ada di contexts/catalog → halaman Share Card error; kini delegasi ke handler (GET `?job=`); (2) **ShareView salah baca param** `?code=` padahal legacy & link modal pakai `?job=CODE` → link yg dihasilkan modal mendarat kosong; kini `?job`; (3) **TabKelola fetch tanpa session token** → `getAppData` admin selalu sessionInvalid (defect class A08) & baris `Loker` tak punya `dokumenShare`/`tsk` → tombol Share dibuka tanpa data simpanan; kini `api.secure` + tipe Loker membawa `dokumenShare`/`tsk`; (4) **pemilihan dokumen TIDAK pernah memuat `dokumenShare` tersimpan maupun menyimpannya** — 4 checkbox hard-coded (CV/JFT/SSW/ALL), tanpa panggilan `updateDokumenShare` (handler backend ada, UI tak pernah memanggil); kini chip penuh legacy `SHARE_DOC_CHIPS` (CV/JFT/SSW/SIM A/KTP/KK/AKTE/IJAZAH/…/ALL, default 'CV,JFT,SSW') pre-check dari `job.dokumenShare` + simpan via `api.secure('updateDokumenShare', [code, joined])`; (5) **WA copas pakai pesan sekali-pakai** bukan template legacy `templateShareWa` (お疲れ様です … KAMI APLOD/UPDATE DI SINI …); kini preview + copas pakai template asli dari kode/pekerjaan job + link share; (6) copy hard-coded & key `toast.*` tak ada → key legacy id+jp (`ui.share_*`, `ui.save_share`, `ui.share_doc_*`, `admin.doc_*`, toast sukses/gagal); (7) klik dalam modal ikut menutup via backdrop (tanpa stopPropagation) → ditambah. +4 test backend (`share-data.test.ts` — handler terdelegasi, `?job` dibaca, guard-less, stub error → 400), +11 test frontend (`AdminShareModal.test.tsx` — parseDocsShare/split 'SIM A' utuh, label-key map, chip list parity, WA template, pre-check dari dokumenShare, toggle, save payload/close/toast, gagal → toast+tanpa close, link `?job=`, chip simpanan di luar daftar tetap tampil); backend 31 file/266 test, frontend 20 file/154 test, typecheck exit 0 |
| A16 | AI Interview simulator VIP (`bukaSimulatorInterview`, `js/ai_copilot/interview.ts` + `#modal-interview`) | `InterviewSimulatorModal.tsx` (BARU — sebelumnya TIDAK ADA; tombol "Latihan Interview" menunjuk `/ai-cv`) | ✅ 2026-09-05 | Root-fix: (1) **fitur TIDAK PERNAH ada di Astro** — tombol "Latihan Interview" CandidateDash adalah `<a href="/ai-cv">` (halaman AI CV Master Assistant — duplikat tombol sebelahnya); legacy `bukaSimulatorInterview` membuka CHAT LANGSUNG dengan Jeklin Sensei (`#modal-interview`). Chat modal di-port penuh (`InterviewSimulatorModal.tsx`): greeting per bidang SSW, typing indicator, bubble user/AI (**bold**, pre-wrap), kirim 20 chat terakhir, penanda `===HASIL===` → JSON hasil → `simpanHasilWawancara`; tombol hijau Selesai → `selesaikanWawancara` (rangkum transcript) → simpan ke admin + bubble ringkasan (skor/nilai/jumlah field biodata/rekomendasi/status kirim); (2) **gate VIP/KELAS hilang** — legacy bukaSimulatorInterview cek `isVipCatatan(catatanInt)` (tag literal `[VIP]` ATAU `[KELAS x]` — regex TIGHTENED, tag lain spt [MCU]/[VISA] TIDAK membuka; backend `_lib/ai/chat.ts` masih regex LAMA yang luas → di-sync ke rule baru via `interview-shared.ts`; helper `canAccessInterview` di-export klien); (3) **backend chat hidup mati** — `processAiInterview` di-enqueue job `ai.interview` (sweep 2 menit) padahal giliran chat interaktif harus real-time spt legacy callAPI (A11 sync admin chat) → kini sinkron lewat surface; (4) **handler tak pernah unwrap args ARRAY** `[{wa,candidateName,history}]` (handler lain unwrap payload[0]) → wa/nama/history DIBAHANG tiap giliran → `unwrapInterviewPayload` + `lastHistory(20)` di `interview-shared.ts`; (5) **semua copy hard-coded** → 21 key baru id+jp (`ui.interview_sim`/`ai_interview_*`/`iv_*` + `admin.interview_ph`; `ui.interview_practice` id di-sinkron ke teks legacy "Latihan Interview"). +11 test backend (`service-a16.test.ts` — helper murni VIP/unwrap/history, surface sync guard anon/admin/refresh pre-DB, unwrap end-to-end: history nyata sampai provider), +13 test frontend (`InterviewSimulatorModal.test.tsx`); backend 32 file/277 test, frontend 21 file/167 test, typecheck exit 0 |
| A17 | Edit loker (`bukaEditFullLoker`/`submitEditFullLoker`, `js/api/jobs.ts` + `#modal-edit-full-loker` ef-*; catatan: `admin_modal/job.ts` ternyata flow LAMAR publik, bukan admin) | `AdminJobEditModal.tsx` | ✅ 2026-09-05 | Root-fix: (1) **field Syarat bernama `syRat` (typo juga di tipe `Job`) padahal backend map `syarat`** → kotak Syarat SELALU terbuka kosong & editnya DIBUANG diam-diam saat simpan (`mapJobPayloadToRow` JOB_COLUMNS cuma kenal `syarat`); kini `syarat` end-to-end (form + payload + tipe shared 4 situs di types/api.ts); (2) **ef-tsk (select TSK pengurus) tidak ada** — pengurus tidak bisa diubah dari edit; kini select berbasis dropdown config `getAppData admin → dropdowns` + union nilai lama (select tidak blank utk nilai non-config); kategori/gender ikut select config+union, lokasi text+datalist (parity ef-lokasi/list-lokasi); (3) **upload ef-template/ef-pamflet tidak ada** — kini dua file input (accept legacy) upload Cloudinary hanya bila dipilih, payload `templateCv`/`pamflet` = URL baru atau `-` (server pertahankan nilai lama krn ''/'-' dihapus handler); (4) **select "Status" tambahan yang TIDAK ada di ef-*** — nilai status job_database mentah ("✅ OPEN"/"❌ CLOSE") sehingga select OPEN/URGENT/CLOSE blank & simpan menimpa nilai mentah; status punya toggle khusus (legacy row action) → select dihapus, form kini cocok ef-* field demi field (tidak lagi kirim key `status`); (5) klik dalam modal ikut menutup via backdrop (tanpa stopPropagation) → ditambah (defect class A15); (6) label hard-coded (TSK/GENDER/KATEGORI/Kode/Save/UPLOAD TEMPLATE/PAMFLET) → key id+jp (`admin.form_tsk/category/gender/job_code_ro/modal_edit_job_title`, `button.save_changes`, `ui.update_cv_template/update_pamflet`); backend `handleEditLokerFull` (guard admin + optimistic concurrency If-Match) sudah benar → tidak berubah. +9 test frontend (`AdminJobEditModal.test.tsx` — prefill job.syarat, dropdown union, payload syarat/tsk tanpa status/syRat + updated_at, edit syarat/tsk tersimpan, upload Cloudinary + payload URL, tanpa file → '-' tanpa upload, error path, label t()); backend tak berubah (32 file/277 test), frontend 22 file/176 test, typecheck exit 0 |
| A18 | DB filter/sort (`filterDbJob`/`setSortDb`/`renderDbFilters`, `js/render/admin.ts` + `js/admin_modal/dbfilter.ts` + `#filter-bidang-container`/`#filter-tahapan-container`) | TabDbJob header filter/sort | ✅ 2026-09-05 | Root-fix: (1) **state filter sudah ada tapi baris chip TIDAK pernah di-render** — `fBidang`/`fTahapan` ada tapi UI tak menampilkan kontrol apa pun (hanya input cari) → kini dua baris chip (`public.all` + opsi) dari dropdown config (`getAppData admin → dropdowns.kategori/.tahapan`) union nilai yg ada di data (parity renderDbFilters); (2) **sort TERBANYAK = no-op** (`return 0`) → kini urut benar DESC jumlah kandidat per job via `buildCandidateCountMap` (idLoker multi-job dipecah per kode — count dihitung utk tiap job; legacy keying string utuh undercount kandidat multi-loker) — juga dipakai sel count baris (sebelumnya `includes()` substring per render); (3) sort tanggal tanpa tie-break → kini parity `filterDbJob`: createdAt DESC/ASC, tie/NaN → code localeCompare; (4) search parity sudah benar (OR includes lower code/tsk/pekerjaan/lokasi, bidang/tahapan eksak) → diekstrak ke helper murni `filterDbJobs`/`sortDbJobs` + di-test; (5) heading/kolom/sort/empty semua hard-coded → 7 key baru id+jp (`admin.history_internal`, `admin.sort*`, `table.action_db`, `db.empty`), nilai dari legacy locales. Catatan insiden sesi: skrip tambah-key A18 menimpa `src/store/i18n.ts` → dict id direkonstruksi (dari jp utuh + 82 nilai legacy + authoring), lalu nilai yg di-drift di-restore eksak dari script pass A12/A15 yg selamat + teks yg dipin test A08/A11/A12 (termasuk `changepass.hint`, `ai.parse_ok_title`, `share_*`, `star_hint`, `rincian_*`); seluruh gates hijau kembali. +6 test frontend (`TabDbJob.test.tsx`); backend tak berubah (32 file/277 test), frontend 23 file/182 test + guard i18n, typecheck exit 0 |
| A19 | CV Digital dossier (`bukaDigitalCV`/`isiEditCepatCv`/`simpanEditCepatCv`/`simpanCatatanCv`, `js/admin_modal/cv.ts` + `#modal-cv`) | `CandidateProfileModal.tsx` (dossier — dibuka dari eye/history row kandidat) | ✅ 2026-09-05 | Koreksi baris: `admin_modal/cv.ts` = modal DOSSIER CV Digital (bukan `InputManualModal` = input manual kandidat, bukan pula `RirekishoBuilder` = preview rirekisho A10). Astro padanannya `CandidateProfileModal` (dossier, data ter-dekorasi dari row). Root-fix: (1) **Edit Data Cepat mengirim data TER-MAP ke `openCandidateEdit`** (fisik digabung "175 / 70", `tmplahir` ≠ `tempatLahir`) padahal `EditCandidateModal` (A03) prefill dari row MENTAH (`tb`/`bb`/`tempatLahir`/`jftText`/`catatanInt`) → editor terbuka SEPARUH kosong (defect class A01–A03); kini row mentah (seed / `getExistingCandidateJsonByWa`) diteruskan apa adanya; (2) **status kandidat tidak ditampilkan** — legacy `cv-status` menampilkan `(tahapan) (status)`; dossier Astro cuma chip tahapan → baris Status kini menampilkan tahapan + status; (3) **seluruh chrome hard-coded** (0 pemakaian `t()`) → 29 key BARU di id+jp (label biodata/JFT/SSW/job/status, `ui.note_internal/external`, `ui.cand_eval`, `ui.loading_candidates`, `ui.age_years_suffix`, tombol VIP on/off, `ui.cv_*` dsb) + `ui.toast_eval_note_saved` jp ditambah + nilai `ui.complete_berkas_biodata` id diselaraskan ke "Lengkapi Pemberkasan & Biodata" (tombol CandidateDash ikut — fungsi sama, wording legacy); toast sukses/gagal/network kini via key. Catatan gap TERDOCUMENTASI (bukan di-silence): **baris password kandidat legacy (`cv-pass`/`passwordDiubah`) TIDAK di-port** — auth Astro S8 menyimpan bcrypt (register wajib password eksplisit, bukan default 4 digit WA; tak ada flag passwordDiubah), jadi menampilkan "4 digit WA" pasti MENYESATKAN; reset password kandidat = fitur identity/auth terpisah. Catatan: backend `handleUpdateCatatanKandidat` sudah menerima format objek dossier & legacy posisi (dua-duanya OK); quick-edit inline legacy digantikan tombol → modal edit penuh (superset sadar). +2 test frontend (`CandidateProfileModal.test.tsx` — raw-row dispatch ke openCandidateEdit + baris status tahapan&status; chrome via key ter-pin); backend tak berubah (32 file/277 test), frontend 23 file/184 test + guard i18n, typecheck exit 0 |

## B. Modal Kandidat/Publik

| # | Modal / fitur | Komponen Astro | Status | Catatan |
|---|---|---|---|---|
| B01 | Login kandidat/admin | `LoginModal.tsx` | ✅ 2026-09-05 | Ground truth: `js/04_auth.ts` (`prosesLoginKandidat`/`prosesLoginMaster`/`prosesLoginPersonal`) + `shared/wa-rules.ts` (normalisasi WA ketat) + modal login/admin. Root-fix: (1) **login admin MATI total** — modal kirim `[pin, token-klien]` (pola legacy) padahal kernel `z.tuple` ARITY EKSAK → `checkAdminMaster`/`checkAdminPersonal` selalu gagal validasi (`Array must contain at most 1/2 element(s)`); kini payload `[pin]` / `[name, pin]` (token bukan bagian payload) — dikunci di test surface backend; (2) **daftar kandidat juga MATI** — modal kirim 3 arg `[nama, wa, password]` tapi `kandidatRegister` tuple 4-slot (zod 3: `.optional()` item TIDAK melonggarkan arity) → `VALIDATION_FAILED` selalu; kernel diubah ke union tuple 2/3/4 arg (caller legacy 2-arg tetap jalan; `usia` diterima tapi tak dipakai identity); (3) **regex WA klien rusak** `/^8d{10,12}$/` ('d' literal, bukan `\d`) → 8xx selalu ditolak; waSchema klien ditulis ulang memirror rule backend `netlify/functions/shared/wa-rules.ts` (ID 628xx 12-15 + JP 81xx 10-15, `normalizeWaInput` → kanonik 628/81) — nomor Jepang 090/070/080 kini diterima (parity rule backend), bukan cuma Indonesia; (4) **`onClose()` dipanggil SAAT RENDER** (side-effect dalam render) → pindah ke `useEffect`; (5) seluruh copy/toast/placeholder hard-coded → 16 key baru id+jp (`login.*`/`admin.*`) + `tErr()` memetakan pesan zod → key (fallback pesan asli). Catatan: daftar akun admin step-2 tetap array statis `[SACHOU, AYOK, KHOLIS, KHOCI]` (parity legacy admin.html yang juga statis) — sumber akun terpusat = pekerjaan C-series. +8 test frontend (`LoginModal.test.tsx` — normalisasi 08xx/8xx/090→81xx, invalid → toast tanpa API, payload arity eksak admin, register payload, onClose via efek; mock store pakai nanostores atom karena `useStore` butuh `.listen`) + +6 test backend (`service-b01.test.ts` — arity eksak ditolak, normalisasi WA di surface sebelum identity, register 2/3/4-arg), backend 33 file/283 test, frontend 24 file/201 test + guard i18n (4), typecheck exit 0 |
| B02 | WA Pintar | `WAPintarModal.tsx` + `TabWA.tsx` | ✅ 2026-09-05 | Ground truth: `js/08_wa_pintar.js` (`injectModalWaPintar`/`bukaModalWaPintar`/`terapkanTemplateWa`/`kirimWaPintar`/`submitWaTemplate`/`editWaTemplate`/`prosesHapusWa`) + `#modal-wa-pintar` + tab admin-wa; template CRUD via `callAPI("simpanWaTemplate", [id, nama, isi, S])`/`hapusWaTemplate` (rute ke endpoint whatsapp) + `getAppData → waTemplates` (parity `window.ALL_WA_TEMPLATES`). Root-fix: (1) **modal tidak pernah bisa dibuka di admin** — WAPintarModal cuma import yatim di CandidateDash (tidak pernah di-render); tombol WA di baris kandidat (TabPelamar) adalah `window.open("https://wa.me/"+wa)` TANPA template/pesan (degradasi legacy `bukaModalWaPintar(idKandidat)` dgn title `ui.send_wa_call`); kini tombol membuka modal smart-sender dgn template picker + normalisasi WA; import yatim dihapus; (2) **TabWA save/delete MATI** — POST body mentah `{nama, isi}`/`{id}` ke `/.netlify/functions/config` (kontrak tak ada); kontrak benar `simpanWaTemplate [id?, nama, isi]` / `hapusWaTemplate [id]` (admin-guarded, surface notify) via `api.secure`; (3) `alert()`/`confirm()`/`location.reload()` → `showToast` + refetch in-place (parity showToast + refreshDataDinamis); (4) **seluruh copy hard-coded** (modal + tab) → 29 key baru id+jp dari locale legacy (`ui.wa_open_send`, `ui.manual_or_template`, `ui.toast_wa_invalid_cand2`, `ui.send_wa_call`, `ui.toast_wa_template_saved`, `ui.manage_wa_templates`, `ui.template_*`, `ui.featured_badge`, `ui.invite_class_wa_desc`, `ui.confirm_delete_template`, `ui.kandidat_tujuan` dsb) + nilai `ui.wa_pintar` jp dikoreksi ke legacy "WAスマート" (sebelumnya "WAテンプレート") + jp `ui.toast_cand_not_found` ditambah; render template pakai JSX (auto-escape — legacy F1 XSS terhindar). Backend tak berubah (handler sudah benar). Catatan i18n: penambahan via skrip atomik read-modify-write (aturan A19/B01); 2 insiden skrip (nilai jp ter-mix ke dict id + blok terselip di luar objek `id`) → diperbaiki dgn rebuild terverifikasi; typecheck/guard hijau. +6 test frontend (`WAPintarModal.test.tsx` — placeholder <<NAMA>>/<<JOB>>, select kosong, toast invalid/empty, wa.me ter-encode + close, chrome via key), +5 test frontend (`TabWA.test.tsx` — load/simpan/edit/hapus via api.secure + toast + refetch, chrome via key); backend tak berubah (33 file/283 test), frontend 26 file/212 test + guard i18n, typecheck exit 0 |
| B03 | Preview dokumen | `DocumentPreviewModal.tsx` | ✅ 2026-09-05 | Ground truth: `js/init/preview.ts` (`previewFileInFrame` — SATU pintu preview inline) + `js/03_candidate.ts` (`bukaPreviewDokumen`/`setStatusBerkas`: gambar/PDF native→gview wrapper FIX 2026-08-19, CSV→SheetJS lokal, Office→MS viewer, zip/dll→pesan+Unduh anti auto-download, Drive folder→tab fallback) + dossier `#modal-cv` (BUKA CV/JFT/SSW/FOTO). Root-fix: (1) **seluruh chrome DocumentPreviewModal hard-coded** → key id+jp (`ui.preview_loading/unavailable/unavailable_hint/load_failed/admin_only_download/download`) + jp ditambah utk `doc_preview_title`/`preview_loading`/`preview_unavailable` (nilai legacy); (2) **Drive folder link belum ditangani** → fallback tab baru + tutup (parity `bukaPreviewDokumen`); (3) **PemberkasanModal "Sudah (Lihat)" = `<a target=_blank>`** — legacy `setStatusBerkas` komentar eksplisit "Preview INLINE (modal) — bukan buka tab baru" (PDF sering ter-download di mobile, masalah yg gview wrapper selesaikan) → kini tombol membuka DocumentPreviewModal inline; (4) **dossier `#modal-cv` kehilangan tombol BUKA CV/JFT/SSW/FOTO** — row ter-dekorasi membawa `cvUrl/jftUrl/sswUrl/pasPhoto` tapi `mapApiToCandidate` membuangnya (key `ui.open_*` ada sejak A19 tapi tak terpakai) → baris Dokumen baru dgn 4 tombol → preview inline. Ikon: file-alt/file-pdf/camera (sprite ada; `image` tidak dipakai krn `certificate` tiada di sprite). +10 test frontend (`DocumentPreviewModal.test.tsx` 8 — routing gambar/PDF gview/Office/fallback zip/previewOnly/drive folder/keyed chrome; `CandidateProfileModal.test.tsx` +2 — baris Dokumen + klik BUKA CV → gview, tanpa dokumen → baris tak dirender) + test A05 lama di-update (link→tombol preview); backend tak berubah (33 file/283 test), frontend 27 file/222 test + guard i18n, typecheck exit 0 |
| B04 | Detail loker (publik) | `LokerDetailModal.tsx` | ✅ 2026-09-05 | Ground truth: `js/01_public.ts` `bukaDetailLoker()` (render+aksi: pamflet thumb klik→`bukaPamflet`, gender badge, kuota, total+rincian, syarat trOption, keterangan, tombol Format/`lamarJob`/WA dari `ASSETS.SOCIAL.whatsapp`) + `jobTutupUntukLamar` (SATU aturan dipakai list & detail) + `render/public.ts`. Root-fix: (1) **`jobTutupUntukLamar` duplikat 2× dengan set fase-open BERBEDA** — modal membuang `LIST-CHECK/PENCARIAN/PENDAFTARAN/DAFTAR` (loker tahapan PENCARIAN/DAFTAR menampilkan tombol Lamar DISABLED walau rekrutmen masih jalan; list juga tanpa LIST-CHECK) → aturan kanonik legacy-eksak diekstrak ke `src/lib/jobPhase.ts`, dipakai modal+list (test pin set open/closed legacy); (2) **pamflet di modal statis** — legacy klik→`bukaPamflet` zoom + title `ui.click_zoom` (list sudah begitu) → kini wired ke PamfletModal; (3) **apply baris list MATI** — `openForm` memanggil `generateFormBridge` yg handler-nya masih menunjuk `/apply-full.html` (halaman legacy YANG TIDAK ADA di Astro) → 404 atau fallback WA; legacy punya SATU aksi (`lamarJob`) utk baris+detail → baris kini konvergen ke rute native `/apply?job=` yang dipakai modal. Semua key i18n modal sudah ada di kedua dict (guard hijau, tanpa edit dict). +10 test frontend (`jobPhase.test.ts` 4 — pin list fase legacy; `LokerDetailModal.test.tsx` 6 — fase PENCARIAN/DAFTAR tetap bisa Lamar, FLIGHT disabled, zoom pamflet buka/tutup, keyed chrome, close); backend tak berubah (33 file/283 test), frontend 29 file/232 test + guard i18n, typecheck exit 0 |
| B05 | Pamflet | `PamfletModal.tsx` | ✅ 2026-09-05 | Ground truth: `js/08_wa_pintar.ts` `bukaPamflet`/`tutupPamflet` (guard `!url\|\|url==='-'` → no-op; buka = set src + unhide; tutup = hide + clear src 300ms) + shell `#pamfletModal` (index.html; × aria via `data-lang-aria="public.close"`) + CSS `.modal-content-pamflet` (contain, max-w 700px, max-h 90vh, radius 16px, shadow hitam). Pemakai: baris list + detail `bukaDetailLoker` (keduanya zoom → sudah lengkap di Astro: LokerTable + LokerDetailModal). Root-fix: (1) **aria-label tombol × hard-coded Inggris "Close"** — legacy melokalisasi via `public.close` → kini `t('public.close')` (kedua dict sudah punya key: id "Tutup"/jp "終了"; tanpa edit dict); (2) **klik DI MANA SAJA di dalam overlay menutup zoom & × memanggil onClose 2×** — container dalam tak pernah `stopPropagation` sehingga `onBackdropClick` (onClose) ikut terbakar oleh klik tombol/gambar (legacy hanya tutup lewat ×) → container dalam kini `stopPropagation`: hanya backdrop sungguhan yang menutup, × tepat 1×. +5 test frontend (`PamfletModal.test.tsx` — guard url kosong/'-'/tutup, geometri legacy + aria keyed (bukan "Close"), × tepat 1×, backdrop-close, fade-in setelah load); test B04 disesuaikan (zoom ditutup via aria `public.close`). Backend tak berubah (33 file/283 test), frontend 30 file/237 test + guard i18n, typecheck exit 0 |
| B06 | Share viewer (TSK) | `ShareView.tsx` + `share.astro` | ✅ 2026-09-05 | Ground truth: `share.html` + `js/pages/share.ts` (renderGrid/submitSelection/showError/updateStaticText — fetch `/api/share-data?job=`) + `js/render/share.ts` (link/template) + deep-doc. Root-fix: (1) **ShareView baca field ciptaan (`id/nama/wa/photo/cvUrl/jftUrl/…`) padahal API mengembalikan `id_kandidat/nama_lengkap/no_wa/pas_photo/file_cv/jft/ssw/nilai_jft_text/bidang_ssw_text/extraDocs` + `job {code,name,tsk}`** — SETIAP kartu render nama/gender/wa kosong & tanpa tombol dokumen; kini diadaptasi ke kontrak nyata + parity kartu legacy (foto klik→zoom + fallback avatar, chip gender/usia/tb/bb, chip `nilai_jft_text`/`bidang_ssw_text`, tombol CV/JFT/SSW + 1 tombol per dokumen ekstra folder via `src/lib/shareDocs.ts` port `docTypeOf`/label legacy, seleksi seluruh kartu w/ aria-pressed + cek pojok); (2) **"Kirim Pilihan" membuka `wa.me` TANPA NOMOR + pesan sekali-pakai** → kini pesan legacy (greet + `*CODE - NAME*`, daftar `N. Nama (ID: …)`, penutup) ke `wa.me/6287889502004`; (3) **gate per-job TOKEN (P1 §5, keputusan user: lazy-mint + wajib + stabil)** — token di `sys_config` (type `share_token`, key=code) via `_lib/db/shareTokens.ts`; `handleShareData(jobCode, tk)` MENOLAK `?job` polos / token salah / job belum di-share; `updateDokumenShare` & `getShareTokenForJob` baru (admin-guarded, surface jobs + apiEndpoint + MUTATING) mint-sekali & mengembalikan token; `share-data.js` meneruskan `?tk=`; AdminShareModal muat token saat buka + bawa `&tk=` di link/WA template/preview (A15 di-update ke kontrak token); (4) filter/usia/JFT pakai semantik legacy (PEREMPUAN→p, usia 0 bukan <20, A2/N4 & B1/N3); (5) bahasa ikut `langStore` (`asj_lang`, satu kunci dgn legacy) & chrome → 20 key baru id+jp `share.*` (nilai persis locale legacy; insiden skrip: blok id terselip di luar objek `id` → direpair dgn verifikasi, guard hijau). +6 test backend (`service-b06.test.ts` gate DB-free + `share-data.test.ts` ?job+?tk), +13 test frontend (`ShareView.test.tsx` 6 — kontrak API nyata, tombol per dokumen, fallback avatar, pesan WA+nomor, error view, empty; `shareDocs.test.ts` 6 — docTypeOf/label legacy; `AdminShareModal.test.tsx` di-update ke kontrak token); backend 34 file/289 test, frontend 32 file/250 test + guard i18n, typecheck exit 0 |
| B07 | Cek data siswa (dari landing) | `CekSiswaModal.tsx` (shared) | ✅ 2026-09-04 | A01 |

## C. Form / Alur (per halaman) — wiring form→backend

| # | Halaman | Status | Catatan |
|---|---|---|---|
| C01 | apply-full → `submitApply` | ✅ 2026-09-05 | A1/A2 selesai sesi 2026-09-04 (action submitApply + payload flat + parse success). A3 polish sesi ini: cekRiwayat kini prefill gender/usia/tb/bb + simpan oldDocs (photoUrl/jftUrl/sswUrl dari `cekDataPelamar`) dan payload submit membawa `oldPhoto/oldCv/oldJft/oldSsw` (backend fallback saat tidak upload baru — pelamar lama tidak kehilangan dokumen); A4 selesai: kartu wajib kini berasal dari job.dokumenShare server (getAppData public → helper murni requiredDocsFromJob di src/lib/applyDocs.ts; delta PARITY_QA A4) — tabel hardcoded JOB_PARAMS (2 kode) dibuang, bidang fallback dari kategori job, dokumen tambahan (KTP/dll) jadi kartu extra → extraFiles payload (backend persist ke keterangan; gate server dokumen_share tetap). A5 selesai: draft localStorage-only asj_apply_<job> (parity legacy saveDraft/restoreDraft — pola M4, tanpa POST), dipulihkan saat mount, dibersihkan setelah submit sukses |
| C02 | master-full → `submitMasterForm` | ✅ 2026-09-05 | M1/M2: save kini upload Cloudinary (photo/jft/ssw/ijazah*/ktp/kk) → JSON `submitMasterForm` (payload flat camel kontrak backend + array pendidikan/pekerjaan/keluarga + darurat/kenalan); no-op FormData dihapus (false-success pong); draft = localStorage-only `asj_master_<wa>` (M4 parity); parse data.success + pesan error |
| C03 | ai-cv → `submitDataAsj` dkk | ✅ 2026-09-05 | AI2/AI3: saveToDatabase kini upload Cloudinary (foto/jft/ssw/ktp/kk/ijazahSd..univ) → JSON nested ai_data_json (identitas/fisik/medis/wawancara/sertifikasi/kenalan_jepang) via apiClient `submitDataAsj`; backend `handleSubmitDataAsj` unwrap array args (parity A07 simpanDataTtdNaitei); WA wajib + validasi |
| C04 | siswa-baru → `processSiswaAIChat` + `submitDaftarSiswa` | ✅ 2026-09-05 | Ground truth: `siswa-baru.html` + `js/pages/siswa_baru.js` (`xe()`/`Xe()` = sendMessage/saveToDatabase). Root-fix di `SiswaBaruForm.tsx`: (1) **chat 404 (S1)** — POST action `processSiswaAIChat` ke surface REGISTER (`submitDaftarSiswa` endpoint) padahal action itu surface ai → kini `getEndpoint('processSiswaAIChat')` (fungsi ai-chat); (2) **pesan user tidak pernah sampai ke AI** — payload array-of-one `[{message, history, biodata}]` padahal handler baca objek `{history, currentData}` → turn user sekarang DI-APPEND ke history (slice 20, parity `v.slice(-20)`) dan payload objek dikirim apa adanya (kontrak legacy `callAPI`); (3) **auto-fill form mati** — komponen baca `data.biodata` padahal handler kirim `data.data` snake_case → merge via map snake→camel; (4) **save no-op (S2/S3)** — multipart FormData → `/ai-form-submit` (di-JSON-parse jadi {} → NOT_IMPLEMENTED, tapi `res.ok` toast "sukses") → kini Cloudinary dulu (ktp/kk/ijazah, `uploadToCloudinary`) lalu JSON objek snake `submitDaftarSiswa` (register, publik tanpa session); (5) **hanya nama yang wajib** — legacy `saveToDatabase` minta SEMUA 9 field biodata + 3 scan & menampilkan daftar yg kurang → cek lengkap + toast daftar; (6) greeting/typing/error/sukses/gagal/header daftar-kurang → nilai legacy-eksak (id+jp), **9 key baru** (`siswa.chat_error/success_btn/upload_doc/saving/form_title/form_hint/draft_stale/send/upload_failed`) + **8 nilai di-sync ke legacy** (greeting, success, failed, network_error, analyzing, missing_header/footer, placeholder_chat — ellipsis legacy). Backend tak berubah (34 file/289 test), frontend 33 file/256 test (+1 file/+6: `SiswaBaruForm.test.tsx` 6 — rute ai-chat + payload objek + history berisi turn baru, merge data.data snake→camel, bubble error, submit kurang-data tanpa API, submit lengkap Cloudinary→register tanpa header auth + toast sukses + draf dibersihkan, gagal server → toast `siswa.failed`), guard i18n (4) hijau, dict id 1094/jp 1014 tanpa dupe. |
| C05 | Dokumen/berkas kandidat (upload/simpan/revisi) | 🔄 surface docs sudah di-wire sesi ini; UI modal A belum dicek 1:1 | |
| C06 | Share viewer | ✅ 2026-09-05 (via B06) | P1 tuntas: token per-job (lazy-mint stabil, sys_config) + verifikasi server-side + viewer di-wire ke kontrak API |

## D. Buttons lintas tab (crosscheck saat buka tiap tab/modal di atas)

Simpan/panggil, filter/sort, CSV export, muat-lebih, WA template simpan, kirim massal,
pengumuman (sys_config), migrasi (admin ops), switch tab — **dicakup per baris A/B saat
crosscheck** (cek disabled/event/action).

## Progres
- 2026-09-04: checklist dibuat; A01/B07 selesai (CekSiswaModal root-fixed); C05 backend wired; C01 sebagian.
- 2026-09-04 (lanj.): A02 selesai (CandidateProfileModal root-fixed — data seed + fallback benar,
  simpan evaluasi/VIP live, tag kelas vs VIP konsisten legacy, refresh otomatis). +6 test backend
  (`service-a02.test.ts`), +1 test frontend; backend 24 file/222 test, frontend 6 file/48 test hijau.
- 2026-09-04 (lanj.): A03 selesai (EditCandidateModal + InputManualModal root-fixed — submit
  JSON+action, upload Cloudinary→persist, payload super-edit lengkap dgn pendidikan/catatanExt/VIP
  internal, field prefill mapCandidate, kolom Catatan = ext‖admin). +3 test backend (patch-builder),
  +1 test frontend; backend 24 file/225 test, frontend 6 file/49 test hijau.
- 2026-09-04 (lanj.): A04 selesai (ListKandidatModal root-fixed — sumber data penuh
  `fetchAllKandidat`, tombol dossier legacy, payload undang-grup object + worker `wa.broadcast`
  diimplementasikan di sweep-queue [sebelumnya NOT_IMPL → undangan tak pernah terkirim],
  remove via tandaiGagalJob + refresh). +5 test frontend (ListKandidatModal); frontend 7 file/54
  test, backend 24 file/225 test hijau.
- 2026-09-04 (lanj.): A05 selesai (PemberkasanModal root-fixed — jenis berkas kanonik backend via
  berkasCatalog (dulu mayoritas dokumen diam-diam tidak di-persist), simpanBiodataLengkap live
  (dulu NOT_IMPL di surface; kini handler master-data ber-guard + surface MASTER_ACTIONS),
  checklist Sudah/Belum + auto-fill biodata + gating tahapan + konfirmasi timpa + retry serial,
  dan adapter CandidateDash ke bentuk getAppData asli `candidates[0]` + refresh candidates-changed
  (dulu baca `kandidatData` yang tak pernah ada → dash kosong). +7 test backend (`service-a05.test.ts`),
  +4 test frontend (PemberkasanModal); backend 25 file/232 test, frontend 8 file/58 test hijau.
- 2026-09-04 (lanj.): A06 selesai (UndanganKelasModal root-fixed — parse daftar ortu kini
  normalisasi 08xx/8xx→628xx via shared/wa-rules (dulu baris valid legacy dibuang); broadcast
  massal di rebuild async-accepted (job queue) → modal lama laporkan "0 terkirim"; kini antrean
  + polling getJobStatus & hasil job di-persist oleh recordJobResult di sweep-queue (sebelumnya
  completeJob membuang hasil handler); + i18n keys & placeholder; ikon hyperscript `link`/
  `stopwatch`/`comment-dots` masuk sprite (scanner diperluas ke `h(Icon,{name:…})` + regenerate);
  mapping apiEndpoint getJobStatus ditambah). +8 test frontend (UndanganKelasModal); frontend
  9 file/66 test, backend 25 file/232 test hijau.
- 2026-09-04 (lanj.): A07 selesai (TTD/esign root-fixed — rebuild lama cuma 1 area tanda tangan
  `saveSignature`→ttd1; kini `EsignNaiteiModal` port penuh `#modal-ttd` legacy: 4 area ttd+nama
  2 pihak, layar gambar penuh canvas, submit `simpanDataTtdNaitei` sekali; handler backend di-guard
  kandidat(admin boleh, dulu kandidat-only) + terima ARRAY args callAPI maupun objek GAS legacy
  (dulu hanya objek → wa tak pernah ketemu via HTTP), scope owner-or-admin & IDOR dipertahankan;
  tombol E-Sign CandidateDash di-wire + gating tahapan regex legacy; i18n keys; icon sprite
  diperluas (file-signature/pen/users/eraser/dst; komponen menghindari literal nama ikon di
  dalam ekspresi spt `tone === 'sky'` → false-positive scanner turun 2→1); ESignatureModal lama
  jadi orfan). +6 test backend (`service-a07.test.ts`), +8 test
  frontend (EsignNaiteiModal); frontend 10 file/74 test, backend 26 file/238 test hijau.
- 2026-09-05: A08 selesai (Ganti password root-fixed — Astro `changePassword` menulis ISO
  timestamp ke kolom boolean `password_diubah` → PATCH selalu gagal (legacy `true`); kini
  `buildPasswordPatch` pure → `{password_kandidat: hash, password_diubah: true}`; klien dulu
  raw fetch tanpa session token → selalu 'Akses ditolak.'; kini `apiClient`; guard disempitkan
  ke kandidat-owner-only sesuai legacy (admin/IDOR/refresh → sessionInvalid, DB-free); schema
  zod `newPasswordField` 6–20 tanpa spasi (server+klien); error klien `data.message`;
  copy → `changepass.*` i18n + hint legacy + key ok). +8 test backend (`service-a08.test.ts`),
  +6 test frontend (ChangePasswordModal); backend 27 file/246 test, frontend 12 file/84 test,
  typecheck exit 0.
- 2026-09-05 (lanj.): A09 selesai (CV mini root-fixed — modal buka KOSONG + raw fetch tanpa
  session token (selalu sessionInvalid) + pendidikan free-text + foto key `photo` yang dibuang
  diam-diam oleh handler (MASTER_FILE_COLUMNS butuh `photoFile`→pas_photo; bug yg juga hidup di
  legacy) + tanpa refresh pasca-simpan; kini prefill dari row kandidat ter-dekorasi (gender
  dinormalisasi PRIA/L→LAKI-LAKI, WANITA/P→PEREMPUAN; usia/tb/bb digit-only; helper murni
  di-export), select pendidikan tetap legacy (SMA/SMK/MA/D3/S1; '-' tidak dikirim), api.secure,
  photoFile Cloudinary → pas_photo benar-benar persist, dispatch candidates-changed, copy i18n
  legacy id+jp). +5 test backend (`service-a09.test.ts` — guard alias + anon/refresh/IDOR/WA
  kosong DB-free), +7 test frontend (CvMiniModal); backend 28 file/251 test, frontend
  13 file/91 test, typecheck exit 0.
- 2026-09-05 (lanj.): A10 selesai (Preview CV/Rirekisho root-fixed — tombol kandidat "Preview
  Desain CV" membuka DocumentPreviewModal URL KOSONG (tak ada yg tampil); kini membuka
  RirekishoBuilder engine renderCVAjaib; foto fallback pas_photo baris kandidat ditambahkan
  (legacy ALL_CANDIDATES); BUG PORT regex kehilangan backslash: fmtMonthYearJp `\d`→`d`
  literal (tahun saja jadi `2024年1月`) & strip non-digit `/D/g` + nomor rirekisho `(d{3,})`
  tak jalan → dikoreksi ke `\d`/`\D`; loading copy → t('ui.loading')). +15 test frontend
  (`helpers_cv.test.ts`, `RirekishoBuilder.test.tsx`); frontend 15 file/106 test, backend
  28 file/251 test, typecheck exit 0. Catatan residual: korupsi regex serupa masih ada di
  `public/LokerTable.tsx` fallback-sort (`replace(/D/g)` — di luar unit A10, butuh cek legacy).
- 2026-09-05 (lanj.): A11 selesai (Admin AI Copilot root-fixed — chat bubble TIDAK PERNAH
  dirender (state ada, JSX hanya typing indicator); SEMUA aksi raw fetch tanpa session token →
  selalu sessionInvalid; `parseDokumenBiodata` di-route ke job background `ingest.parse` yang
  worker-nya NOT_IMPL (handler asli di `_lib/ai/classify.ts` orfan) → parse tak pernah jalan,
  dan parse legacy DUA langkah (`submitMasterForm`) tidak ada — data hasil parse dibuang;
  kini: bubble dirender (esc+**bold**), semua aksi `api.secure`, surface ingest panggil handler
  asli sinkron, dua-langkah dipulihkan + refresh `candidates-changed`; candidateId di-wire
  benar (TabPelamar kirim `id`, AdminPanel tak lagi kirim nama); hasil wawancara parity legacy
  (updatedAt/nilai/bio-field/backfill WA); 18 key i18n baru id+jp; residual A10 `LokerTable.tsx`
  `/D/g`→`/\D/g` ikut dibereskan. +6 test backend (`service-a11.test.ts`), +9 test frontend
  (`AdminAiCopilot.test.tsx` + boldHtml); backend 29 file/257 test, frontend 16 file/115 test,
  typecheck exit 0.
- 2026-09-05 (lanj.): A12 selesai (Rincian Biaya & Tahapan builder root-fixed — tombol
  "Buka Editor Rincian" TabTambah MATI tanpa onClick & `rincian_biaya` tak pernah disimpan;
  `AdminJobEditModal` tanpa field total/rincian; preset endpoints backend (`getRincianPresets`/
  `saveRincianPreset`/`deleteRincianPreset`) tak pernah dipanggil UI; TabTambah kirim raw
  multipart POST action fiktif `submitFormAdmin` (tidak ada di surface — backend map
  `simpanJobBaru`). Kini: komponen baru `RincianBiayaModal.tsx` (port `#modal-rincian-builder`
  + `js/13_rincian_builder.ts`) dgn helper murni serialize/parse round-trip stabil thd parser
  publik `LokerDetailModal.parseRincianBiaya`, preset DB load + favorite save/delete
  (fallback DEFAULT_PRESETS legacy), TabTambah + AdminJobEditModal simpan/ubah
  `totalBiaya`+`rincianBiaya` via `api.secure('simpanJobBaru'/'editLokerFull')`; ~21 key i18n
  baru id+jp. +5 test backend (`service-a12.test.ts` guard preset DB-free), +10 test frontend
  (`RincianBiayaModal.test.tsx`); backend 30 file/262 test, frontend 17 file/125 test,
  typecheck exit 0.
- 2026-09-05 (lanj.): A13 selesai (Laporan bulanan root-fixed — modal TIDAK PERNAH memanggil
  backend; dulu meng-agregasi store `kandidatList` klien (halaman aktif ±20–50 baris saja),
  jadi laporan = subset tak lengkap & selalu beda dari legacy; header totalCandidates +
  tanggal generatedAt dan empty-state tidak ada; semua copy hard-coded. Kini `api.secure`
  ('getMonthlyReport') → render kartu per-loker dgn chip tahapan+status (parity legacy),
  header Total: N · tanggal, empty/error/loading state; 6 key i18n baru id+jp
  (`admin.monthly_report`/`report_title`/`report_total`/`report_by_stage`/`report_by_status`/
  `report_empty`, nilai JP = assets/jp-locale.js legacy), tombol pemicu ikut t(). Backend
  `handleGetMonthlyReport` = port setia legacy → tidak berubah. +6 test frontend
  (`LaporanBulananModal.test.tsx`); frontend 18 file/131 test, backend 30 file/262 test,
  typecheck exit 0.
- 2026-09-05 (lanj.): A14 selesai (Matchmaking AI root-fixed — rule legacy "belum terdaftar di
  Job ini" (`idLoker` memuat kode job) tidak ada sama sekali → blast bisa menawari kandidat yg
  sudah ada di job tsb; kelengkapan sertifikat dibaca dari field FILE (`jft`/`ssw`) padahal
  legacy dari TEKS nilai `jftText`/`sswText` (sort/badge/aturan wajib salah); gender auto-fill
  tidak kenal PRIA/WANITA (hanya LAKI/PEREMPUAN); hasil tak dibatasi 30; blast raw fetch tanpa
  session + tanpa linkGrup/customMessage/konfirmasi. Kini: rule 1b + teks sertifikat + helper
  `genderFromJob`/`hasCert` di-export, cap 30, `api.secure('kirimTawaranMassal')` dgn payload
  kontrak legacy + `window.confirm`, ~26 key i18n baru id+jp + 3 key toast offer yg hanya ada
  di id kini di jp juga; tombol Match TabDbJob → `t('admin.btn_match')`. +12 test frontend
  (`MatchmakingModal.test.tsx`); frontend 19 file/143 test, backend 30 file/262 test, typecheck
  exit 0.
- 2026-09-05 (lanj.): A15 selesai (Share settings job root-fixed — endpoint share view
  `netlify/functions/share-data.js` masih stub NOT_IMPLEMENTED padahal handler nyata
  `handleShareData` sudah ada di contexts/catalog → halaman Share Card error total; ShareView
  baca `?code=` padahal legacy & link modal pakai `?job=CODE`; TabKelola fetch tanpa session
  token (getAppData admin selalu sessionInvalid) & baris Loker tak punya `dokumenShare`/`tsk`;
  pemilihan dokumen tidak pernah memuat simpanan `dokumenShare` maupun menyimpannya (4 checkbox
  hard-coded, tanpa panggilan `updateDokumenShare`); WA copas pakai pesan sekali-pakai bukan
  template legacy `templateShareWa`; klik dalam modal ikut menutup via backdrop tanpa
  stopPropagation. Kini: share-data delegasi ke `handleShareData` (GET `?job=`), ShareView +
  link `?job=`, TabKelola `api.secure` + field `dokumenShare`/`tsk`, chip legacy
  `SHARE_DOC_CHIPS` pre-check dari simpanan + save `api.secure('updateDokumenShare')`,
  template WA asli + preview/copas, ~key i18n id+jp. +4 test backend (`share-data.test.ts`),
  +11 test frontend (`AdminShareModal.test.tsx`); backend 31 file/266 test, frontend 20
  file/154 test, typecheck exit 0.
- 2026-09-05 (lanj.): A16 selesai (Simulator Wawancara VIP root-fixed — fitur TIDAK
  PERNAH ada di Astro: tombol "Latihan Interview" CandidateDash = `<a href="/ai-cv">`
  (duplikat tombol AI CV Master); gate VIP/KELAS (legacy isVipCatatan TIGHTENED: [VIP]
  literal atau [KELAS x], bukan tag apa pun) hilang & backend masih regex luas;
  `processAiInterview` di-enqueue job sweep 2 menit (chat interaktif harus live); handler
  tak pernah unwrap args ARRAY → wa/nama/history dibuang tiap giliran; copy hard-coded.
  Kini: `InterviewSimulatorModal.tsx` baru (port `#modal-interview` + interview.ts:
  greeting per bidang SSW, HASIL marker → simpanHasilWawancara, Selesai →
  selesaikanWawancara + ringkasan), tombol CandidateDash di-wire + gate
  `canAccessInterview` (toast ui.toast_feature_locked), `interview-shared.ts` (rule VIP
  + unwrap + history 20) dipakai backend & di-test, surface processAiInterview sinkron,
  21 key i18n baru id+jp + `ui.interview_practice` id disinkron ke legacy. +11 test backend
  (`service-a16.test.ts`), +13 test frontend (`InterviewSimulatorModal.test.tsx`); backend
  32 file/277 test, frontend 21 file/167 test, typecheck exit 0.
- 2026-09-05 (lanj.): A17 selesai (Edit loker full root-fixed — field Syarat
  bernama `syRat` (typo juga bocor ke tipe `Job` 4 situs) padahal backend map
  `syarat` → kotak selalu kosong & edit dibuang saat simpan; select ef-tsk
  (pengurus) tidak ada; upload ef-template/ef-pamflet tidak ada; select
  "Status" tambahan menimpa nilai mentah "✅ OPEN" dll → dihapus (parity ef-*,
  status via toggle sendiri); klik dalam modal menutup via backdrop; label
  hard-coded. Catatan: `admin_modal/job.ts` = flow lamar publik, admin edit
  ada di `js/api/jobs.ts`. Kini: `syarat` end-to-end, kategori/gender/tsk
  select berbasis dropdown config + union nilai lama, lokasi text+datalist,
  upload template/pamflet opsional Cloudinary ('-' → server pertahankan),
  stopPropagation, 8 key i18n baru id+jp. +9 test frontend
  (`AdminJobEditModal.test.tsx`); backend tak berubah (32 file/277 test),
  frontend 22 file/176 test, typecheck exit 0.
- 2026-09-05 (lanj.): A18 selesai (DB filter/sort root-fixed — state filter ada tapi
  chip TIDAK pernah dirender (hanya input cari), TERBANYAK = no-op `return 0`,
  count baris pakai `includes()` substring per render, sort tanggal tanpa tie-break,
  heading/kolom hard-coded). Ground truth: `js/render/admin.ts filterDbJob` (search OR
  includes code/tsk/pekerjaan/lokasi; bidang/tahapan eksak; TERBANYAK = count
  kandidat DESC; tie/NaN → code localeCompare) + `admin_modal/dbfilter.ts
  renderDbFilters` (chip dari DROPDOWNS.kategori/.tahapan). Kini: helper murni
  `buildCandidateCountMap`/`filterDbJobs`/`sortDbJobs` + chip dua baris (config
  dropdown union nilai data), TERBANYAK nyata, count multi-job idLoker dipecah per
  kode (perbaikan sadar vs legacy yg keying string utuh), 7 key i18n baru id+jp
  (`admin.history_internal`, `admin.sort*`, `table.action_db`, `db.empty`). INSIDEN
  sesi: skrip tambah key menimpa `src/store/i18n.ts` (0 byte) → dict id direkonstruksi
  dari jp utuh + legacy id (82) + authoring (58); kemudian nilai yg di-drift di-restore
  eksak dari script pass A12/A15 yg selamat (24 nilai: `changepass.*`/`star_hint`/
  `stage_*`/`share_*`/`fav toast`/`alert.network`/dll) + teks legacy yg dipin test
  A08/A11/A12 (`ai.tab_chat`, `ai_upload_label`, `ai_btn_model`, `parse_ok_title`
  = "Parse berhasil" — cocok `parse.ts` legacy, `custom_item_ph`); frontend sempat
  8 merah → hijau lagi. +6 test frontend (`TabDbJob.test.tsx` — count map multi-job,
  search/bidang/tahapan, TERBANYAK DESC, date tie-break, immutable); backend tak
  berubah (32 file/277 test), frontend 23 file/182 test + guard i18n, typecheck exit 0.
- 2026-09-05 (lanj.): A19 selesai (CV Digital dossier root-fixed). Koreksi baris: `admin_modal/cv.ts` = #modal-cv DOSSIER (`bukaDigitalCV`/quick-edit/simpan catatan) —
  bukan `InputManualModal`/`RirekishoBuilder`; padanan Astro = `CandidateProfileModal`. Root-fix: (1) Edit Data Cepat mengirim data TER-MAP
  (fisik gabung, tmplahir≠tempatLahir) → EditCandidateModal prefill separuh kosong; kini row mentah diteruskan; (2) status kandidat tak pernah
  tampil (legacy cv-status = tahapan+status) → baris Status baru; (3) seluruh chrome hard-coded (modal tanpa `t()` sama sekali) → 29 key id+jp +
  jp `toast_eval_note_saved` + nilai `complete_berkas_biodata` id diselaraskan ke "Lengkapi Pemberkasan & Biodata". Gap terdokumentasi: baris
  password legacy tidak di-port (auth Astro bcrypt S8, tidak ada default 4 digit/flag passwordDiubah — menampilkan default pasti menyesatkan;
  reset = fitur identity/auth terpisah). Backend `updateCatatanKandidat` sudah dukung format objek & posisi legacy (tidak berubah).
  +2 test frontend (`CandidateProfileModal.test.tsx` — raw-row dispatch + status bar + chrome keyed); backend tak berubah (32 file/277 test),
  frontend 23 file/184 test + guard i18n, typecheck exit 0.
- 2026-09-05 (lanj.): B01 selesai (Login kandidat/admin root-fixed) — unit pertama seri B. Dua flow login MATI end-to-end: (1) admin —
  modal kirim `[pin, token-klien]` (pola legacy) vs kernel `z.tuple` arity eksak → `checkAdminMaster`/`checkAdminPersonal` selalu gagal
  validasi; (2) daftar kandidat — modal kirim 3 arg vs tuple 4-slot (zod 3 `.optional()` tidak melonggarkan arity) → `VALIDATION_FAILED`
  selalu. Fix: payload `[pin]`/`[name, pin]`; `kandidatRegister` jadi union tuple 2/3/4 arg (caller legacy 2-arg tetap jalan, `usia`
  diterima tapi tak dipakai identity). Plus regex WA klien rusak `/^8d{10,12}$/` ('d' literal) → ditulis ulang mirroring rule backend
  `shared/wa-rules.ts` (ID 628xx 12-15 + JP 81xx 10-15; 090/070/080 kini valid); `onClose()` dari dalam render → `useEffect`; copy
  hard-coded → 16 key id+jp + `tErr()` peta pesan zod. Catatan: daftar akun admin step-2 tetap array statis (parity admin.html legacy
  yang juga statis; sumber akun terpusat = C-series). +8 test frontend (`LoginModal.test.tsx`), +6 test backend (`service-b01.test.ts`),
  backend 33 file/283 test, frontend 24 file/201 test + guard i18n, typecheck exit 0.
- 2026-09-05 (lanj.): B02 selesai (WA Pintar root-fixed) — `js/08_wa_pintar.js` → `WAPintarModal` + `TabWA`.
  Modal tidak pernah bisa dibuka di admin (WAPintarModal import yatim di CandidateDash; tombol WA baris kandidat =
  wa.me polos tanpa template) → trigger di-port ke TabPelamar (title `ui.send_wa_call`); TabWA save/delete MATI
  (POST body mentah ke /.netlify/functions/config — kontrak tak ada) → `api.secure('simpanWaTemplate', [id?, nama, isi])` /
  `('hapusWaTemplate', [id])` (admin-guarded, surface notify); alert/reload → toast + refetch; 29 key id+jp dari locale
  legacy + nilai `ui.wa_pintar` jp → "WAスマート". Backend tak berubah (handler sudah benar). Insiden i18n sesi:
  skrip penambah key sempat menulis nilai jp ke dict id + blok terselip di luar objek `id` → diperbaiki rebuild
  terverifikasi (typecheck + guard hijau). +11 test frontend (`WAPintarModal.test.tsx` 6, `TabWA.test.tsx` 5);
  backend 33 file/283 test, frontend 26 file/212 test + guard i18n, typecheck exit 0.
- 2026-09-05 (lanj.): B03 selesai (Preview dokumen root-fixed) — `js/init/preview.ts` +
  `bukaPreviewDokumen`/`setStatusBerkas` (03_candidate.ts) → `DocumentPreviewModal`.
  Seluruh chrome modal hard-coded → key id+jp (nilai legacy; jp utk preview_loading/dll ditambah);
  Drive folder link → fallback tab baru (parity bukaPreviewDokumen); PemberkasanModal "Sudah (Lihat)"
  = <a target=_blank> padahal legacy eksplisit preview INLINE ("bukan buka tab baru") → tombol buka
  DocumentPreviewModal; dossier `#modal-cv` kehilangan BUKA CV/JFT/SSW/FOTO (cvUrl/jftUrl/sswUrl
  dibuang mapApiToCandidate; key ui.open_* tak terpakai sejak A19) → baris Dokumen + preview inline.
  +10 test frontend; backend tak berubah (33 file/283 test), frontend 27 file/222 test + guard i18n,
  typecheck exit 0.
- 2026-09-05 (lanj.): B04 selesai (Detail loker publik root-fixed) — `js/01_public.ts`
  `bukaDetailLoker()` → `LokerDetailModal` (host: LokerTable di index/public). Tiga akar masalah:
  (1) `jobTutupUntukLamar` diduplikasi 2× dengan set fase-open BERBEDA — modal membuang
  LIST-CHECK/PENCARIAN/PENDAFTARAN/DAFTAR (loker tahapan PENCARIAN/DAFTAR menampilkan Lamar
  DISABLED walau masih rekrut) → aturan kanonik legacy-eksak diekstrak ke `src/lib/jobPhase.ts`
  (dipakai modal+list); (2) pamflet modal statis padahal legacy klik→`bukaPamflet` zoom
  (title ui.click_zoom, list sudah) → wired ke PamfletModal; (3) tombol Lamar di baris list MATI:
  `openForm` → `generateFormBridge` → backend masih mengarah `/apply-full.html` (halaman legacy
  tak ada di Astro) → 404 atau fallback WA; legacy SATU aksi lamarJob utk baris+detail → baris
  konvergen ke rute native `/apply?job=` yang dipakai modal. Semua key i18n sudah ada (guard
  hijau, tanpa edit dict). +10 test frontend (jobPhase.test.ts 4 pin fase legacy-eksak +
  LokerDetailModal.test.tsx 6: PENCARIAN/DAFTAR terbuka, FLIGHT disabled, zoom pamflet, chrome);
  backend tak berubah (33 file/283 test), frontend 29 file/232 test + guard i18n, typecheck exit 0.
- Urutan kerja usulan berikutnya: C02 (master, no-op FormData M1/M2) → C03 (ai-cv, no-op
  FormData AI2/AI3) → C01 (apply polish) → C05 — update baris ini tiap sesi.
- 2026-09-05 (lanj.): B05 selesai (Pamflet root-fixed) — `js/08_wa_pintar.ts`
  `bukaPamflet`/`tutupPamflet` + shell `#pamfletModal` → `PamfletModal`. Geometri
  Astro sudah persis CSS `.modal-content-pamflet` (contain/700px/90vh/radius 16/shadow).
  Root-fix: (1) aria tombol × hard-coded Inggris "Close" → `t('public.close')` (parity
  data-lang-aria legacy; key sudah ada di kedua dict); (2) container dalam tidak
  stopPropagation → klik apa pun di dalam overlay (termasuk gambar) menutup zoom dan
  tombol × membakar onClose 2× (legacy hanya menutup lewat ×) → container dalam kini
  stopPropagation, hanya backdrop yang menutup, × tepat 1×. +5 test frontend
  (PamfletModal.test.tsx: guard url kosong/'-'/tutup, geometri + aria keyed, × 1×,
  backdrop, fade-in); test B04 disesuaikan ke aria `public.close`. Backend tak berubah
  (33 file/283 test), frontend 30 file/237 test + guard i18n, typecheck exit 0.
- 2026-09-05 (lanj.): B06 selesai (Share viewer TSK root-fixed — P1 §5 & row C06 tuntas) —
  `share.html` + `js/pages/share.ts` → `ShareView` + `share.astro`. Tiga akar: (1) ShareView
  membaca field ciptaan (`id/nama/wa/photo/cvUrl`) padahal API mengembalikan `id_kandidat/`
  `nama_lengkap/no_wa/pas_photo/file_cv/jft/ssw/nilai_jft_text/bidang_ssw_text/extraDocs` +
  `job {code,name,tsk}` → kartu kosong semua; kini adaptasi kontrak nyata + parity kartu legacy
  (foto zoom/avatar fallback, chip gender/usia/tb/bb/JFT/SSW, tombol CV/JFT/SSW + dokumen ekstra
  via `src/lib/shareDocs.ts`, seleksi seluruh kartu aria-pressed); (2) "Kirim Pilihan" buka wa.me
  TANPA nomor + pesan sekali-pakai → pesan legacy + `wa.me/6287889502004`; (3) gate per-job token
  (keputusan user: lazy-mint + WAJIB + stabil) — token di sys_config (`shareTokens.ts`),
  `handleShareData(code, tk)` tolak ?job polos/salah/belum di-share, `updateDokumenShare` +
  `getShareTokenForJob` baru mint & kembalikan token, AdminShareModal bawa `&tk=` di link/
  WA template/preview; filter & bahasa parity legacy (`langStore` asj_lang). +6 test backend
  (service-b06 gate DB-free + share-data ?job+?tk), +13 test frontend (ShareView 6, shareDocs 6,
  AdminShareModal token). Backend 34 file/289 test, frontend 32 file/250 test + guard i18n,
  typecheck exit 0. Insiden skrip i18n: blok id terselip di luar objek `id` (klas B02) → direpair
  dgn verifikasi posisi, count, dupe, jp⊆id.
- 2026-09-05 (lanj.): C04 selesai (siswa-baru root-fixed — S1 chat 404, S2/S3 save no-op) —
  `siswa-baru.html` + `js/pages/siswa_baru.js` (`xe()`/`Xe()`) → `SiswaBaruForm`. Akar-akarnya:
  (1) chat POST action `processSiswaAIChat` ke surface REGISTER (submitDaftarSiswa endpoint) →
  fungsi ai-chat via `getEndpoint('processSiswaAIChat')`; (2) payload array-of-one + field
  `message` yang handler-nya abaikan → turn user di-append ke `history` (slice 20) & payload
  objek `{history, currentData}` dikirim langsung (kontrak legacy callAPI); (3) auto-fill baca
  `data.biodata` padahal handler kirim `data.data` snake → merge snake→camel; (4) submit
  FormData→/ai-form-submit (no-op yang toasts sukses) → upload Cloudinary ktp/kk/ijazah lalu
  JSON objek snake `submitDaftarSiswa` (register, publik); (5) wajib isi semua 9 field + 3 scan
  dgn daftar yg kurang (parity `saveToDatabase`); (6) copy → nilai legacy-eksak id+jp: 9 key baru
  + 8 nilai di-sync. Backend tak berubah (34 file/289 test), frontend 33 file/256 test
  (+1 file/+6: `SiswaBaruForm.test.tsx`), guard i18n (4) hijau, dict id 1094/jp 1014 tanpa dupe,
  typecheck exit 0. Berikutnya: C02 (master) → C03 (ai-cv) → C01 (apply polish) → C05.
- 2026-09-05 (lanj.): C02 master-full + C03 ai-cv root-fixed (M1/M2 no-op FormData, AI2/AI3 no-op FormData) —
  kedua form mengirim raw FormData TANPA action ke dispatcher JSON (master-data / ai-form-submit) →
  action=ping no-op pong HTTP 200 → toast sukses padahal TIDAK tersimpan (false-success). Kini parity
  legacy: (1) MasterFullForm.submitMaster upload tiap file ke Cloudinary dulu (photo/jft/ssw/ijazahSd/
  ijazahSmp/ijazahSma/univ/ktpFile/kk → key MASTER_FILE_COLUMNS) lalu POST JSON `submitMasterForm`
  (payload flat camel kontrak backend + array pendidikan/pekerjaan/keluarga + daruratNama/daruratWa/
  kenalan*); parse data.success + pesan error; tombol Draft = localStorage-only `asj_master_<wa>`
  (M4 parity, tidak POST); (2) AiCvForm.saveToDatabase upload dokumen (foto/jft/ssw/ktp/kk/ijazahSd..
  univ → fotoFile/jftFile/... ) lalu apiClient `submitDataAsj` dgn payload nested ai_data_json
  (identitas/fisik/medis/wawancara/sertifikasi/kenalan_jepang + context.wa); WA wajib + validasi; (3)
  backend `handleSubmitDataAsj` unwrap ARRAY args (pattern simpanDataTtdNaitei A07) — sebelumnya
  menerima payload sebagai objek langsung shg HTTP tak pernah jalan. Verifikasi: typecheck exit 0,
  backend 34 file/289 test hijau, frontend 33 file/256 test hijau (2 timeout terisolasi lulus ulang
  — flaky load). Berikutnya: C01 apply polish (A3 old* refill, A4 requiredDocs server, A5 draft) → C05.


- **2026-09-05 (hardening C02/C01 setelah audit adversarial):** (1) Kolisi status_eks_jepang: MASTER_COLUMN_MAP memetakan eksJepang DAN lamaJepang ke kolom yang sama → lamaJepang (tahun, step 2) menimpa pilihan status (step 5) saat keduanya terisi = korupsi kolom. Fix: lamaJepang dilepas dari map kolom, dialihkan ke ai_data_json.wawancara.lama_di_jepang via buildAiOverflow/mergeAiOverflow (skema chat.ts). (2) buildMasterBody (pure builder DB-free) menggantikan loop inline: slot keluarga/pendidikan/pekerjaan yang kosong TIDAK ditulis → simpan ulang form tidak menghapus baris lama (keluarga_1_usia dulu selalu ditimpa string kosong). (3) Kartu keluarga MasterFullForm: input TTL/Gender/Alamat/WA (tanpa kolom DB → hilang diam-diam) diganti field Usia (parity legacy: nama/hubungan/usia/pekerjaan); payload kini mengirim usia. (4) Dead code dibuang: isDraft di payload (selalu false — draft lokal-only), import apiClient tak terpakai, oldDocs.cv/oldCv (cekDataPelamar tidak pernah mengembalikan URL CV). Verifikasi: typecheck exit 0, backend 35 file/298 test hijau (+9 test service-c02), frontend 33 file/256 test hijau.

- **2026-09-05 (prefill C02):** MasterFullForm kini memuat data master yang sudah ada via getDrafCvMaster (buildMasterNested) setelah gate login — form tidak mulai kosong, edit tidak menimpa kolom yang tidak disentuh. buildMasterNested mengekspos kunci baru untuk prefill: no_coe, tgl_terbit/exp/kota paspor, kontak_darurat_nama/hubungan, lama_di_jepang (dari ai_data_json.wawancara). Mapper pure mapMasterNestedToForm di src/lib/masterPrefill.ts (testable); draft localStorage (asj_master_<wa>) tetap menang untuk data, dokumen lama ditampilkan. Verifikasi: typecheck exit 0, backend 35 file/300 test (+2 buildMasterNested), frontend 33 file/263 test (+7 masterPrefill).
- 2026-09-05 (C01 A4/A5): kartu wajib apply-full kini dari server (job.dokumenShare via getAppData public — helper murni requiredDocsFromJob, JOB_PARAMS hardcoded dibuang, bidang fallback kategori, dokumen tambahan jadi kartu extra → extraFiles). Draft apply = localStorage-only asj_apply_<job> (legacy saveDraft/restoreDraft, pola M4 — tanpa POST), dipulihkan saat mount, dibersihkan setelah sukses. Verifikasi: typecheck exit 0, frontend 37 file/281 test hijau (+8 applyDocs, +5 ApplyFullForm), backend 35 file/300 test hijau.
- 2026-09-05 (correctness pass error-path C02/C03/C04): (1) FIX label error per tahap di SiswaBaruForm — phaseBefore ditangkap di entry (selalu idle saat klik pertama) sehingga upload gagal di-label siswa.network_error dan save-stage network error di-label siswa.upload_failed (terbalik); kini marker lokal failedStage di-set sebelum tiap tahap → upload gagal → siswa.upload_failed + pesan asli, save network gagal → siswa.network_error. (2) Kontrak error dedup uploadMany kini ter-pin di CI (sebelumnya hanya line-verified — AiCvForm/SiswaBaruForm mock uploadMany tanpa throw, MasterFullForm tanpa test file sama sekali): MasterFullForm.test.tsx BARU (+3: gate tanpa sesi tanpa API; upload gagal → toast Gagal upload photo + pesan eksak + setSaving(false) tombol aktif lagi + TANPA submitMasterForm; draft M4 localStorage-only tanpa POST), AiCvForm +1 (upload gagal → toast eksak + tanpa submitDataAsj), SiswaBaruForm +2 (upload gagal → upload_failed + tanpa register; save network gagal → network_error). Verifikasi: typecheck exit 0, frontend 40 file/297 test hijau (+1 file/+6), backend 35 file/300 test hijau.
- 2026-09-05 (design pass C02/C03/C04): duplikasi kontrak upload dokumen dikolapskan ke satu owner. Map file-key→payload-key (FILE_TO_PAYLOAD di MasterFullForm + DOC_TO_PAYLOAD di AiCvForm, identik kata-demi-kata) kini di src/lib/documentColumns.ts (MASTER_FILE_COLUMNS/AI_FILE_COLUMNS/SISWA_FILE_COLUMNS) — menambah jenis dokumen cukup 1 file klien + map backend MASTER_FILE_COLUMNS (tidak disentuh). Loop iterate→Cloudinary→collect-URLs di 3 form diganti uploadMany(files, map) di src/lib/cloudinary.ts (uploader injectable utk test; error pertama → UploadCollectionError bawa key+message asli) — toast "Gagal upload ...", setSaving(false)+return, dan path error per form byte-identical. +4 test cloudinary.test.ts (skip kosong, urutan+payload-key, error key/message/berhenti, map identitas), +6 test documentColumns.test.ts (pin eksak 3 map + pin set key payload vs kontrak backend). Verifikasi: typecheck exit 0, frontend 39 file/291 test hijau (+2 file/+10), backend 35 file/300 test hijau.
- 2026-09-05 (correctness pass error-path C02/C03/C04): (1) FIX label error per tahap di SiswaBaruForm — phaseBefore ditangkap di entry (selalu idle saat klik pertama) sehingga upload gagal di-label siswa.network_error dan save-stage network error di-label siswa.upload_failed (terbalik); kini marker lokal failedStage di-set sebelum tiap tahap → upload gagal → siswa.upload_failed + pesan asli, save network gagal → siswa.network_error. (2) Kontrak error dedup uploadMany kini ter-pin di CI (sebelumnya hanya line-verified — AiCvForm/SiswaBaruForm mock uploadMany tanpa throw, MasterFullForm tanpa test file sama sekali): MasterFullForm.test.tsx BARU (+3: gate tanpa sesi tanpa API; upload gagal → toast Gagal upload photo + pesan eksak + setSaving(false) tombol aktif lagi + TANPA submitMasterForm; draft M4 localStorage-only tanpa POST), AiCvForm +1 (upload gagal → toast eksak + tanpa submitDataAsj), SiswaBaruForm +2 (upload gagal → upload_failed + tanpa register; save network gagal → network_error). Verifikasi: typecheck exit 0, frontend 40 file/297 test hijau (+1 file/+6), backend 35 file/300 test hijau.

- **2026-09-05 (C03 ai-cv anonymous-save UX):** AiCvForm kini punya login gate pola MasterFullForm. Parity fact: backend minta sesi untuk chat (processAIChat H4 guard) DAN simpan (submitDataAsj admin/kandidat + owner scope), sedangkan apiClient tanpa sesi menampilkan toast + logout + redirect ke / → seluruh state CV hilang. Fix (semua di AiCvForm.tsx): (1) gate saat mount tanpa sesi (WA editable, prefill URL/auth/cv.hp, login via loginKandidat, hp CV diisi dari WA login); (2) guard di saveToDatabase — tanpa sesi buka gate, bukan redirect; (3) chat kini membawa Authorization + body.sessionToken (sebelumnya tidak pernah → chat selalu ditolak server). Verifikasi: tsc exit 0, frontend 35 file/268 test (+5 AiCvForm).

- 2026-09-05 (UX fix publik, live-repro): dua bug yang dilaporkan user (buka pamflet muter-muter terus; toggle ID-JP tidak sat-set) ter-reproduksi di preview + di-fix:
  (1) PamfletModal: gambar disembunyikan (opacity 0) sampai event onLoad, padahal gambar cache bisa selesai SEBELUM Preact memasang listener onLoad —
  bukti live: img complete:true 800x1000 tapi opacity 0 + spinner animate-spin >5 detik; tak ada onError sama sekali. Fix: reset-effect kini men-reconcile via imgRef.complete
  (gambar sudah selesai saat commit -> langsung setLoaded(true)) + handler onError -> spinner tidak pernah muter selamanya; geometri/tombol tutup tidak berubah.
  (2) Race kamus JP (i18n.ts + App.tsx): dict JP dimuat async (import chunk i18n-jp) sementara render + translateDataLang jalan sync setelah langStore.set(jp) —
  bukti live: asj_lang tersimpan jp dan tombol menunjukkan JP tapi teks halaman tetap Bahasa Indonesia (3 detik, tidak pernah flip; baru berganti saat ada re-render tak terkait).
  Fix: loadJp() kini meng-install translations.jp; export ensureJpLoaded() di-await toggleLang sebelum set(jp); setelah dict tiba, jpReady.notify + translateDataLang ulang
  (subscribe preload + init saat lang jp tersimpan) -> switch pertama ke JP langsung berbahasa Jepang.
  (3) Fetch ganda home: marquee <script> dan LokerTable masing-masing POST getAppData ~110KB -> single-flight baru src/lib/publicData.ts (cache in-flight, reset setelah settle) —
  Network live: 1x POST get-app-data per muat home (sebelumnya 2x).
  Test +4: PamfletModal +2 (onError -> opacity 1 + tanpa spinner; gambar cached complete -> tampil tanpa tunggu onLoad), src/store/i18n.test.ts baru +2
  (ensureJpLoaded install dict; load halaman dengan lang=jp -> preload + jpReady true). Verifikasi: typecheck exit 0, frontend 41 file/301 test hijau (+4), backend 35 file/300 test hijau;
  live: toggle JP 17ms dan ID 18ms (dulu tak pernah flip), pamflet cache-hangat muncul (800x1000) tanpa spinner nyangkut.

- 2026-09-05 (QA referensi responsiveness legacy, live diukur): legacy disajikan lokal dari E:Asjpow4v7-mainkhoci921 (static + proxy fungsi ke backend live asjportal.netlify.app) dan di-klik langsung.
  Ukuran: pamflet zoom 28ms tanpa spinner (astro dulu spinner abadi di gambar cache, sudah difix); detail loker 19ms dari data klien; login/daftar ~13-19ms (modal pre-rendered, toggle display);
  toggle bahasa ~185ms terjemahan DOM penuh (termasuk panel tersembunyi + data kolom) vs astro 17-18ms UI publik (cakupan data kolom belum parity); Edit Loker Full 29/20ms; apply-full.html DOMContentLoaded 310ms.
  Temuan: legacy = monolitik semua modal pre-rendered -> buka/tutup hampir selalu <=30ms; latensi dominan = Netlify Functions produksi (sama utk kedua versi). Referensi lengkap: docs/LEGACY_MODALS_RESPONSIVENESS.md.
